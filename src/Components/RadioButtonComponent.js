import React from 'react';

const RadioButtonComponent = ({ hasBeenSubmitted, isChosen, isCorrect }) => {
  return (
    <div>
      <div className="radio-button" />
    </div>
  );
};

export default RadioButtonComponent;
